# Huffman-image-compressor
# overview
Using the Huffman Coding technique, I created a  compression engine in C++.  It reads raw byte data streams into memory using modern C++ vectors, generates frequency distributions with hash maps, and packs the bit pathways into compressed binary files. It successfully optimized the files by 40 KB, demonstrating clean memory design and performance analytics."
